# Screenshot Memory - An iOS / Android / Max OS tool to keep track of your screenshots


Clone the project to your local git folder, install the flutter framework  and run in the folder:

```bash
flutter run
```
